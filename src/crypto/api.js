const encryptData = () => {
  // TODO: logic for encryptData. This takes the whole JSON object.
}

const decryptData = () => {
  // TODO: logic for decryptData
}

module.exports = {
  encryptData,
  decryptData
}
